# Minimal requirements for render-safe video processing
# Only what we absolutely need for FFmpeg-based processing

# Core Flask dependencies
Flask==3.1.1
Flask-CORS==5.0.0
Werkzeug==3.1.3

# Basic utilities (lightweight)
requests==2.32.3
rich==13.9.4

# Production server
gunicorn==23.0.0